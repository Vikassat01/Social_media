const config = {
    apiUrl: 'http://localhost:3000/api',
    endpoints: {
        login: '/auth/login',
        register: '/auth/register',
        save: '/user/save',
        getSaved: '/user/saved'
    }
};

export default config;
